# Base origin of the Hive API. Leave EMPTY for local dev (Vite proxies /api).
# In production (Vercel), set this to your deployed API URL, e.g.:
# VITE_API_BASE=https://hive-api.onrender.com
VITE_API_BASE=
